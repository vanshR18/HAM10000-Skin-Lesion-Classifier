
# 🔬 HAM10000 Skin Lesion Classifier

<p align="center">
  <img src="https://img.shields.io/badge/Python-3.10-blue?style=for-the-badge&logo=python&logoColor=white"/>
  <img src="https://img.shields.io/badge/PyTorch-2.3.0-EE4C2C?style=for-the-badge&logo=pytorch&logoColor=white"/>
  <img src="https://img.shields.io/badge/FastAPI-0.111-009688?style=for-the-badge&logo=fastapi&logoColor=white"/>
  <img src="https://img.shields.io/badge/EfficientNet--B0-Transfer%20Learning-orange?style=for-the-badge"/>
  <img src="https://img.shields.io/badge/Dataset-HAM10000-blueviolet?style=for-the-badge"/>
</p>

<p align="center">
  A deep learning pipeline that classifies dermoscopy images into <strong>7 skin lesion categories</strong> using transfer learning on the HAM10000 dataset — with a full REST API built on FastAPI.
</p>

---

## 📌 Table of Contents

- [Overview](#-overview)
- [Dataset](#-dataset)
- [Classes](#-classes)
- [Project Structure](#-project-structure)
- [Pipeline](#-pipeline)
- [Installation](#-installation)
- [Running the Notebooks](#-running-the-notebooks)
- [FastAPI Server](#-fastapi-server)
- [API Endpoints](#-api-endpoints)
- [Example Requests](#-example-requests)
- [Example Response](#-example-response)
- [Results](#-results)
- [Tech Stack](#-tech-stack)
- [Disclaimer](#-disclaimer)

---

## 🧠 Overview

This project builds an end-to-end skin lesion classification system:

- **EDA** — class imbalance analysis, demographics, pixel statistics
- **Preprocessing** — leak-free splits by `lesion_id`, augmentation, WeightedRandomSampler
- **Baseline CNN** — custom 4-block CNN trained from scratch
- **CNN Model** — EfficientNet-B0 with 2-phase transfer learning (freeze → fine-tune)
- **Evaluation** — confusion matrix, per-class F1, ROC-AUC, Grad-CAM visualizations
- **FastAPI** — REST API serving the trained model with single & batch prediction endpoints

---

## 📂 Dataset

**HAM10000** (*Human Against Machine with 10,000 training images*)  
A large collection of multi-source dermatoscopic images of common pigmented skin lesions.

| Property | Value |
|---|---|
| Total Images | 10,015 |
| Image Size | 450 × 600 px (resized to 224 × 224) |
| Classes | 7 |
| Source | [Kaggle — HAM10000](https://www.kaggle.com/datasets/kmader/skin-cancer-mnist-ham10000) |

> Download from Kaggle and place in the following structure:
> ```
> ham10000/
> ├── HAM10000_images_part_1/
> ├── HAM10000_images_part_2/
> └── HAM10000_metadata.csv
> ```

---

## 🏷️ Classes

| Code | Full Name | Type |
|------|-----------|------|
| `nv` | Melanocytic Nevi |  Benign |
| `mel` | Melanoma |  Malignant |
| `bkl` | Benign Keratosis | Benign |
| `bcc` | Basal Cell Carcinoma |  Malignant |
| `akiec` | Actinic Keratosis / Intraepithelial Carcinoma |  Malignant |
| `vasc` | Vascular Lesions |  Benign |
| `df` | Dermatofibroma | Benign |

> ⚠️ The dataset is **heavily imbalanced** — `nv` accounts for ~67% of all images. This is handled via `WeightedRandomSampler` + class-weighted `CrossEntropyLoss`.

---

## 📁 Project Structure

```
ham10000-classifier/
│
├── notebooks/
│   ├── 01_EDA_Preprocessing.ipynb       ← EDA + preprocessing pipeline
│   └── 02_Baseline_CNN_Evaluation.ipynb ← model training + evaluation
│
├── api/
│   └── main.py                          ← FastAPI application
│
├── outputs/                             ← generated by notebooks
│   ├── train.csv
│   ├── val.csv
│   ├── test.csv
│   ├── dataset_meta.json
│   └── *.png                            ← all plots and charts
│
├── models/                              ← generated by notebooks
│   ├── baseline_best.pth
│   ├── efficientnet_best.pth
│   └── efficientnet_inference.pth       ← used by FastAPI
│
├── ham10000/                            ←   dataset
│
├── test_api.py                          ← API test script
├── requirements.txt
└── README.md
```

---

## 🔄 Pipeline

```
Raw Images + CSV
      │
      ▼
01_EDA_Preprocessing.ipynb
  ├── Exploratory Data Analysis
  │     ├── Class distribution (imbalance check)
  │     ├── Age / Sex / Localization analysis
  │     ├── Sample image grid per class
  │     └── Pixel channel statistics
  │
  └── Preprocessing
        ├── Missing value imputation
        ├── Label encoding
        ├── Leak-free split by lesion_id (80 / 10 / 10)
        ├── WeightedRandomSampler
        ├── Augmentation (flip, rotate, colorjitter, affine)
        └── Saves → outputs/train.csv, val.csv, test.csv, dataset_meta.json
              │
              ▼
02_Baseline_CNN_Evaluation.ipynb
  ├── Baseline CNN (4-block from scratch)
  ├── EfficientNet-B0
  │     ├── Phase 1 — frozen backbone, train head only
  │     └── Phase 2 — unfreeze all, differential LR fine-tuning
  │
  └── Evaluation
        ├── Classification report (per-class)
        ├── Confusion matrix
        ├── Per-class Precision / Recall / F1
        ├── ROC-AUC curves (one-vs-rest)
        ├── Grad-CAM visualizations
        ├── Misclassification analysis
        └── Saves → models/efficientnet_inference.pth
              │
              ▼
        FastAPI Server
          ├── POST /predict       (single image)
          └── POST /predict/batch (up to 10 images)
```

---

## ⚙️ Installation

**Python 3.10+ recommended**

```bash
# 1. Clone the repository
git clone https://github.com/vanshR18/ham10000-classifier.git
cd ham10000-classifier

# 2. Create a virtual environment (recommended)
python -m venv venv
source venv/bin/activate        # Linux / Mac
venv\Scripts\activate           # Windows

# 3. Install dependencies
pip install -r requirements.txt
```

**`requirements.txt`**
```
torch==2.3.0
torchvision==0.18.0
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pillow==10.3.0
numpy==1.26.4
pandas
scikit-learn
matplotlib
seaborn
tqdm
opencv-python
pydantic==2.7.1
```

---

## 📓 Running the Notebooks

Open JupyterLab or Jupyter Notebook and run in order:

```bash
jupyter lab
```

### Notebook 1 — EDA & Preprocessing
```
notebooks/01_EDA_Preprocessing.ipynb
```
- Update `BASE_DIR` in **Cell 2** to point to your local `ham10000/` folder
- Run all cells top to bottom
- Outputs saved to `outputs/`

### Notebook 2 — Model Training & Evaluation
```
notebooks/02_Baseline_CNN_Evaluation.ipynb
```
- Automatically loads `outputs/` from Notebook 1
- Trains Baseline CNN (15 epochs) then EfficientNet-B0 (10 + 20 epochs)
- Saves the final model to `models/efficientnet_inference.pth`

> 💡 **GPU strongly recommended.** Training on CPU will be very slow.  
> On an NVIDIA GPU, expect ~30–45 min total training time.

---

## 🚀 FastAPI Server

Once Notebook 2 is complete and `models/efficientnet_inference.pth` exists:

```bash
# Start the server
uvicorn api.main:app --reload --host 0.0.0.0 --port 8000
```

The API will be live at:

| URL | Description |
|-----|-------------|
| `http://localhost:8000` | API root |
| `http://localhost:8000/docs` | 📖 Interactive Swagger UI |
| `http://localhost:8000/redoc` | 📄 ReDoc documentation |

---

## 🔗 API Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| `GET` | `/` | Welcome message + endpoint list |
| `GET` | `/health` | Model status, device, uptime |
| `GET` | `/classes` | All 7 classes with descriptions |
| `POST` | `/predict` | Single image prediction |
| `POST` | `/predict/batch` | Batch prediction (up to 10 images) |

---

## 📤 Example Requests

### Single Image — `curl`
```bash
curl -X POST "http://localhost:8000/predict" \
  -H "accept: application/json" \
  -F "file=@skin_image.jpg"
```

### Single Image — Python
```python
import requests

with open("skin_image.jpg", "rb") as f:
    response = requests.post(
        "http://localhost:8000/predict",
        files={"file": ("skin_image.jpg", f, "image/jpeg")}
    )

data = response.json()
print(f"Predicted  : {data['predicted_class']} — {data['full_name']}")
print(f"Confidence : {data['confidence_pct']}")
print(f"Malignant  : {data['is_malignant']}")
print(f"Warning    : {data['malignant_warning']}")
```

### Batch Prediction — Python
```python
import requests

files = [
    ("files", ("img1.jpg", open("img1.jpg", "rb"), "image/jpeg")),
    ("files", ("img2.jpg", open("img2.jpg", "rb"), "image/jpeg")),
    ("files", ("img3.jpg", open("img3.jpg", "rb"), "image/jpeg")),
]

response = requests.post("http://localhost:8000/predict/batch", files=files)
data = response.json()

print(f"Total images : {data['total_images']}")
print(f"Total time   : {data['total_time_ms']} ms\n")

for result in data["results"]:
    print(f"{result['filename']:<25} → {result['predicted_class']}  ({result['confidence_pct']})")
```

---

## 📊 Example Response — `/predict`

```json
{
  "filename": "ISIC_0024306.jpg",
  "predicted_class": "mel",
  "full_name": "Melanoma",
  "confidence": 0.873412,
  "confidence_pct": "87.34%",
  "is_malignant": true,
  "malignant_warning": "⚠️ Potentially malignant lesion detected. Please consult a dermatologist.",
  "all_probabilities": [
    { "class_key": "mel",   "full_name": "Melanoma",                             "probability": 0.873412, "percentage": "87.34%" },
    { "class_key": "nv",    "full_name": "Melanocytic Nevi",                     "probability": 0.082100, "percentage": "8.21%"  },
    { "class_key": "bkl",   "full_name": "Benign Keratosis",                     "probability": 0.021300, "percentage": "2.13%"  },
    { "class_key": "bcc",   "full_name": "Basal Cell Carcinoma",                 "probability": 0.013200, "percentage": "1.32%"  },
    { "class_key": "akiec", "full_name": "Actinic Keratosis / Intraepithelial Carcinoma", "probability": 0.006700, "percentage": "0.67%"  },
    { "class_key": "df",    "full_name": "Dermatofibroma",                       "probability": 0.002400, "percentage": "0.24%"  },
    { "class_key": "vasc",  "full_name": "Vascular Lesions",                     "probability": 0.000888, "percentage": "0.09%"  }
  ],
  "inference_time_ms": 18.42
}
```

---

## 🧪 Running Tests

A test script is included that hits every endpoint and validates responses:

```bash
# Run with synthetic test images (no real image needed)
python test_api.py

# Run with a real dermoscopy image
python test_api.py --image path/to/your/image.jpg
```

Expected output:
```
═══════════════════════════════════════════════════════
  HAM10000 API TEST SUITE
  Target: http://localhost:8000
═══════════════════════════════════════════════════════

  ✅  Root returns 200
  ✅  Health returns 200
  ✅  Model loaded = True
  ✅  Returns 7 classes
  ✅  Predict returns 200
  ✅  Predicted class : mel (Melanoma)
  ✅  Confidence      : 87.34%
  ...

  ✅ Pass : 18   ❌ Fail : 0
```

---

## 📈 Results

| Model | Test Accuracy | Macro F1 | Macro ROC-AUC |
|-------|:---:|:---:|:---:|
| Baseline CNN (scratch) | ~0.62 | ~0.45 | ~0.88 |
| EfficientNet-B0 (transfer) | ~0.83 | ~0.72 | ~0.96 |

> Actual numbers will vary slightly depending on your hardware, random seed, and training duration. Results above are representative of expected performance.

---

## 🛠️ Tech Stack

| Category | Tool |
|---|---|
| Language | Python 3.10 |
| Deep Learning | PyTorch 2.3, torchvision |
| Model | EfficientNet-B0 (ImageNet pretrained) |
| Data Processing | NumPy, Pandas, scikit-learn |
| Visualization | Matplotlib, Seaborn |
| Explainability | Grad-CAM (custom implementation) |
| API Framework | FastAPI + Uvicorn |
| Image Processing | Pillow, OpenCV |
| Notebooks | JupyterLab |

---

## ⚠️ Disclaimer

> This project is built for **research and educational purposes only.**  
> It is **not a certified medical device** and must **not** be used for clinical diagnosis or treatment decisions.  
> Always consult a qualified and licensed dermatologist for medical advice.

---

<p align="center">
  Made with ❤️ using PyTorch & FastAPI
</p>
